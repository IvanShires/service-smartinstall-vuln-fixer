# service-smartinstall-vuln-fixer
Removes the SmartInstall feature on any switch as input (IP address).. If it fits into the category where it WILL support "no vstack", it will do that, otherwise - create an ACL. 
